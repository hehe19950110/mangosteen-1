import { defineComponent } from 'vue';
import style from './FloatBttton.modele.scss';

export const FloatButton = defineComponent({ 
  setup : (props,context) => {
    return () => (
      <div>  </div>
    )
  }
})